# kami-engine-sculpt

Non-destructive SDF sculpting engine. Brush strokes are immutable EDN operations; meshing is delegated to the existing mesher boundary so undo, collaboration, and deterministic export retain their semantics.
